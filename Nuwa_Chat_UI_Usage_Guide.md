# 女媧對話介面 (Nuwa Chat UI) 使用指南

這份指南將引導您如何使用「女媧對話介面」進行聊天，以及如何擴充與更新您的本地技能庫（角色庫）。

## 1. 環境需求與初始設定

在開始使用之前，請確保您的系統符合以下條件：
*   **Python 環境**：確保您已經安裝了 Python (需要隱含執行 `build_data.py` 將 Markdown 檔轉為網頁可讀格式)。
*   **Gemini API Key**：本系統使用 Google 的 Gemini 模型，您需要準備一組有效的 API Key (例如來自 Google AI Studio)。

## 2. 開啟對話介面

1. 前往女媧技能存放目錄：`C:\Users\etrny\.gemini\antigravity\scratch\nuwa-skill\`。
2. 找到 `nuwa_chat.html` 檔案，**對其連點兩下**（預設會以您的瀏覽器開啟此本地檔案）。
3. 首次開啟時，系統會要求您輸入 **Gemini API Key**。
    * 請在左側側邊欄最下方的設定區域輸入您的 API Key 並點擊儲存。
    * API Key 將**只會安全地儲存在您的瀏覽器本地端 (LocalStorage)**，不會上傳到任何第三方伺服器。若想清除，可隨時點擊「清除金鑰」。

## 3. 基本操作方式

*   **挑選角色**：
    您可以在左側的「角色清單」中，點擊挑選您想要對話的角色（例如 Elon Musk Perspective）。選定後，系統將會載入該角色的專屬認知框架與提示詞。
*   **使用 AI Auto-Router (自動選角模式)**：
    如果您選取側邊欄最上方的 **"Auto Select" (自動推薦)** 模式，當您輸入訊息後，系統會自動在背景分析您的問題，從您的整個本地技能庫中挑選最適合的專家角色來進行回覆。
*   **進行對話**：
    在畫面底部的輸入框輸入您的訊息，按下 Enter 或點擊發送按鈕。AI 的回覆將支援完整的 Markdown 格式渲染（包含程式碼高亮、表格、粗體等）。
*   **匯出對話紀錄**：
    點擊輸入框旁邊的 **「Export Chat」 (匯出) 按鈕**，即可將當前的對話完整打包成 Markdown (`.md`) 檔案下載到您的電腦中，方便日後參考或歸檔。

---

## 4. 如何擴充與更新角色庫？

本系統採用模組化設計。當您想要為系統添加新的角色視角 (Perspective) 或是修改既有角色時，請依循以下三個步驟：

### 步驟一：建立或修改角色檔案
1. 進入角色的存放目錄：`C:\Users\etrny\.gemini\antigravity\scratch\nuwa-skill\examples` (或者其他有 `SKILL.md` 的所在位置)。
2. 為新角色**建立一個新資料夾**（例如：命名為 `steve-jobs-perspective`）。
3. 在該資料夾內，建立一個命名為 `SKILL.md` 的純文字檔案。
4. 使用標準的 Frontmatter (`---`) 及 Markdown 語法撰寫角色定義。

**`SKILL.md` 範例格式：**
```markdown
---
name: Steve Jobs Perspective
description: 採用 Steve Jobs 的視角與標準來思考產品設計與使用者體驗。
author: YourName
version: 1.0.0
---

# System Prompt
你現在是 Steve Jobs。在回答問題時，請展現對極致細節的追求，且強調「設計不是外表，而是它怎麼運作」。
請用繁體中文進行回覆，態度要強勢且具啟發性。
```
*(請確保 `name` 及 `# System Prompt` 這兩個段落存在，這是系統抓取名稱與系統提示詞的核心資訊)。*

### 步驟二：執行更新腳本 (非常重要！)
**您每次新增、修改或刪除任何資料夾內的 `SKILL.md` 之後，都必須重新打包資料，網頁才能讀取到最新變更。**

1. 回到 `nuwa-skill` 根目錄 (`C:\Users\etrny\.gemini\antigravity\scratch\nuwa-skill\`)。
2. 找到名為 **`update_skills.bat`** 的批次檔。
3. **連點兩下執行它**。
4. 終端機視窗將會閃現，若資料轉換無誤，即代表打包完成。
5. *(說明：這支腳本會在背景以 Python 執行 `build_data.py`，掃描所有的 `SKILL.md` 並將它們統整成一個 `skills_data.js` 供網頁使用。)*

### 步驟三：重新整理網頁
回到您的瀏覽器中，對著已開啟的 `nuwa_chat.html` 頁面**按下 `F5` 重新整理**。
接著查看左側的「角色清單」，您應該就會看見剛剛新增或修改的角色已經成功套用了！如果您選擇 Auto Select，AI 也會將您的新角色納入挑選考量中。

---

## 5. 進階技巧：如何產生您的個人背景資料 (User Context)？

我們在「⚙️ API 設定」中提供了「使用者背景資料」的填寫欄位，這能讓 AI 更深入了解您，提供高度客製化的建議。
若您不知道該怎麼寫，您可以使用以下這段**專門為萃取個人背景而設計的 Prompt**。

**操作方法：**
您可以把這段指令貼給平常最常使用的 AI（如 ChatGPT、Claude、或本專案中的 Gemini），讓它從您過去的對話歷史中萃取出精華，再把萃取出的結果直接貼入我們的系統設定中！

**萃取專用 Prompt：**
```text
你的工作是瀏覽我們過去的對話，然後總結你對我的瞭解。
請勿在輸出內容中使用第一人稱代名詞 (我、我的) 和第二人稱代名詞 (你、你的)，而是改用「使用者」或中性用語，稱呼你從資料中瞭解的這個對象。
盡可能保留使用者說的每一字句，特別是指令與偏好設定。

類別 (照以下順序輸出)：
1. 使用者資訊：慣用名稱、職業、教育程度和居住地。
2. 興趣與偏好：持續積極參與的活動 (不只是擁有某個物品或購買過一次的東西)。
3. 關係：已確認的長期關係。
4. 標註日期的事件、專案與計畫：近期重大活動的記錄。
5. 指令：我明確要求你在之後遵守的規則，包括「哪些一定要做」、「哪些絕對不能做」，以及對你之前行為的更正。僅納入個人化記憶中的規則，不含對話內容。

格式：
將內容依上述類別分段。盡量引用我在提示詞說過的原話，證明你總結的各個項目。按照下列格式建立各個項目：
使用者的名字是<name>。
- 證據：使用者說「叫我<name>」。日期：[YYYY-MM-DD]。

輸出內容：
- 將最終輸出摘要的格式設為文字區塊。
```
